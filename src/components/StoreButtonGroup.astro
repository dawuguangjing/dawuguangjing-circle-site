---
interface Link {
  label: string;
  href: string;
  store: string;
  track?: string;
}

interface Props {
  links: Link[];
  trial?: boolean;
  location?: string;
}

const { links, trial = false, location } = Astro.props;
const gaEvent = trial ? 'trial_click' : 'purchase_click';
---

<div class="store-button-group">
  {links.map((link) => (
    <a
      class={`store-button${trial ? ' store-button--trial' : ''}`}
      href={link.href}
      target="_blank"
      rel="noopener noreferrer"
      data-store={link.store}
      data-track={link.track}
      data-ga-event={gaEvent}
      data-ga-location={location}
    >
      <span>{link.label}</span>
      <span class="arrow">→</span>
    </a>
  ))}
</div>
